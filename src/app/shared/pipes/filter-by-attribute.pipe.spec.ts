import { FilterByAttributePipe } from './filter-by-attribute.pipe';

describe('FilterByAttributePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAttributePipe();
    expect(pipe).toBeTruthy();
  });
});
